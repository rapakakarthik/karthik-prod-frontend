# deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: amazon
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aws
  template:
    metadata:
      labels:
        app: aws
    spec:
      containers:
        - name: amazon-container
          image: rapakakarthik7/dockerrepo:114 
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: amazon
spec:
  type: NodePort  
  selector:
    app: aws 
  ports:
    - port: 80  
      targetPort: 80  
      nodePort: 30001  