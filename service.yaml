# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: flipkart-service
spec:
  type: NodePort 
  selector:
    app: lob  
  ports:
    - port: 80  
      targetPort: 80  
      nodePort: 30000  